# Session — 2026-07-24 — arch-map

## Done

- **Self-truthful architecture map** (`docs/ARCHITECTURE.md`): a module-level
  Mermaid diagram folded from `getProjectData` — the same graph Sheriff
  enforces with, so the map and the rules cannot disagree. An extracted
  feature (ADR-0006) appears as a subgraph the moment it exists; `greet`
  shows as `{domain, application, testing}` inside `packages/core`.
- **Drift-proof**: `docs/architecture.spec.ts` regenerates in the gate and
  fails when the committed map differs — same doctrine as doc truth. Entry
  points are read from `sheriff.config.ts` (one source, no duplicated list).
- **TDD**: 7 tests on the generator written red-first (cross-module folding,
  Windows separators bridged, feature subgraphs, package-root tag labels,
  determinism, multi-entry merge).
- **Eject regenerates the map** in the same operation — replayed end to end
  in an isolated worktree: ejected gate green, map honestly reduced to two
  nodes and zero edges (reachable-graph semantics).
- **Sonar findings fixed** in a separate `refactor:` commit (Tidy First):
  complexity flattened into `foldedGraphOf`/`packageLines`/`featureLines`,
  explicit sort comparator (S2871), no nested ternary (S3358).
- Wired: `pnpm arch:map`, CLAUDE.md command entry, README bullet,
  `ARCHITECTURE.md` added to the living-docs path check.

## Not done / remaining

- The map is module-level only; no per-file view (deliberate — the module is
  the unit Sheriff reasons about).

## Decisions

- **Code-unit sort order, not `localeCompare`** (against Sonar's suggestion):
  the map is a golden file compared across OSes and locale-aware order varies
  with the ICU build. Documented in the comparator's comment.
- The map draws the graph **reachable from the entry points** — an empty
  nursery or unwired file does not appear. That is the same honesty contract
  as `check:arch`, stated in the doc header.
- Module watch: `pnpm modules:hint` — no candidate (nurseries empty).

## Gate status

- typecheck: ✅
- tests (with coverage): ✅ 187 passed, 100 % statements/branches/functions/lines
- mutation (Stryker, local — core untouched, run anyway): ✅ 100.00
- biome / sheriff / knip / jscpd: ✅ all clean
- ejected-skeleton gate (worktree replay, map regenerated): ✅

## State to resume from

- **Single next action**: merge the arch-map PR on green CI; then the first
  real feature (second adapter proving port substitutability).
- Gotchas / half-done edits: none — working tree clean after the report commit.
